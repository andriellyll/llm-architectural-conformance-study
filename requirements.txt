# ── LLM & embeddings ──────────────────────────────────────────────────────────
mistralai>=2.0.0
sentence-transformers>=3.0.0
transformers>=4.40.0
# Note: torch is a transitive dependency of sentence-transformers.
# Install it manually before running pip install -r requirements.txt to avoid
# downloading the large CUDA wheel unnecessarily — see README Setup section.

# ── RAG pipeline ───────────────────────────────────────────────────────────────
llama-index>=0.10.0
llama-index-core>=0.10.0
llama-index-embeddings-huggingface>=0.2.0
llama-index-llms-mistralai>=0.1.0
llama-index-vector-stores-postgres>=0.1.0

# ── Database ───────────────────────────────────────────────────────────────────
pgvector>=0.2.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
SQLAlchemy>=2.0.0

# ── Data processing ────────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
ijson>=3.2.0
beautifulsoup4>=4.12.0

# ── Utilities ──────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0
huggingface_hub>=0.20.0
tqdm>=4.65.0
